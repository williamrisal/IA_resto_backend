services:
  backend:
    build: .
    container_name: paneladmin_backend
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - MONGODB_URI=mongodb+srv://williamrisal14_db_user:nkGIm23GTE74Gf0x@cluster0.no044om.mongodb.net/resto-db?
      - PORT=5000
      - JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-12345
      - CLIENT_URL=http://myrestoia.s3-website.eu-north-1.amazonaws.com
    networks:
      - paneladmin-network
    restart: unless-stopped

volumes:
  mongo_data:
  mongo_config:

networks:
  paneladmin-network:
    driver: bridge
